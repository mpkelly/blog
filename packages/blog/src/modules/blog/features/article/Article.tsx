import * as React from 'react';
import { Row } from 'elements/flex/Row';

const Style = {
  fontFamily: 'primary',
  display: 'block',
  p: {
    lineHeight: 'primary',
  },
};

export const Article = () => {
  return <Row xs={Style} />;
};
