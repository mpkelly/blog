import {
  Check,
  DribbbleLogo,
  X,
  RocketLaunch,
  Wrench,
  Code,
  Image,
} from 'phosphor-react';

import { createIcon } from './Icon';

export const DribbbleIcon = createIcon(DribbbleLogo);

export const CheckIcon = createIcon(Check);

export const CrossIcon = createIcon(X);

export const RocketIcon = createIcon(RocketLaunch);

export const ToolIcon = createIcon(Wrench);

export const CodeIcon = createIcon(Code);

export const ImageIcon = createIcon(Image);
